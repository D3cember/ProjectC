{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/projectc",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "runInTerminal": true,
            "preLaunchTask": "C: gcc build all files"
        }
    ]
}
