#include "headers/data_struct.h"
#include "headers/preproc.h"
#include "headers/errors.h"
#include "headers/globaldefine.h"
#include "headers/util.h"
/*
void second_pass(char *filename ,Symbol *symbol_table){
    int IC = IC_START;
    char line[MAX_LINE_LENGTH];
    FILE *input_file;
    location *amFileSecond;





    input_file = fopen(filename,"r");
    if (!input_file)
    {
        print_internal_error(ERROR_CODE_4);
    }
    amFileSecond = (location *)handle_malloc(sizeof(location));
    if (amFileSecond == NULL)
    {
        print_internal_error(ERROR_CODE_1);
        free(amFileSecond);
        return;
    }

    while (fgets(line,MAX_LINE_LENGTH,input_file)){
        

        
    }
    
    

    

}
*/
